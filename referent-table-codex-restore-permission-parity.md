| 出典 | 目的 | 具体対象 | 役割 | 前後関係 | 初出定義 | 候補語 |
|---|---|---|---|---|---|---|
| ユーザー依頼 | restore後もユーザーが起動したCodexと同じ権限条件を維持する | `[agent_start.commands].codex`に保存された実行ファイルと通常起動オプション | 記録 | 新規paneでCodexを起動する時 | Codex起動argvとは、新規paneとrestoreの両方が共有するCodex実行ファイルと通常起動オプションを指す | Codex起動argv |
| Herdr session restore | 保存済みCodex sessionへ同じ権限条件で再接続する | Codex起動argvの末尾へ`resume`と保存済みsession IDを一度だけ追加したargv | 記録 | Codex sessionをrestoreする時 | Codex restore argvとは、Codex起動argvへsession再接続引数だけを追加したargvを指す | Codex restore argv |
| ユーザー追加依頼 | Claudeが立ち上がっていたpaneを必ず元のconversationへ再接続する | snapshotに保存されたClaude session IDから構築して一度だけ起動する`claude --resume <session-id>`のargv | 記録 | Claude paneのsession IDをsnapshotから復元した後 | Claude restore argvとは、保存済みClaude session IDをClaude CLIへ渡す引数列を指す | Claude restore argv |
| Herdr session restore | Codex/Claudeの復元失敗を一意なsession単位で検出できるようにする | 保存済み`(source, agent, session ID)`ごとに復元計画が一度だけ生成され、対象paneのshell方言に合わせたcommandとしてshellへ渡されたこと | 状態 | snapshot読込後、復元完了を報告する前 | agent session復元成立とは、一意な保存済みsession IDから復元計画を生成し、Unixではaliasを迂回し、WindowsではcmdまたはPowerShellの構文でshellへ渡した状態を指す。同一session IDを持つ後続paneは同時resumeを避けるため抑止する | agent session復元成立 |
| ユーザー回帰報告 | pane右上で会話IDが保存済みかを確認できるようにする | hook authority、persisted session、pending restore planのいずれかにsession参照があるpaneへ表示する`saved session` | 状態 | pane borderを描画する時 | 保存済みsession表示とは、次回restoreに使えるsession参照がserver stateに存在するpaneの右上表示を指す | saved session表示 |
| superseded（現行契約ではない） | 旧案として通常roomの自動配送停止を検討した記録。現行正本はSPEC G9の通常mail direct deliveryとPTY全byte write ack | 旧案の`herdr-jobs`以外をqueuedのまま保持する境界 | 歴史 | `0f61acb`以降のdirect delivery採用前 | 通常room nudge停止は廃止案を識別する語であり、現行実装・受け入れ条件・形式モデルへ流用しない | 通常room nudge停止（superseded） |
| 既存設定 | agent固有の複雑なresume commandを維持する | `[agent_restore.commands].<agent>`に明示されたlegacy shell template | 記録 | Codex起動argvが未設定の時 | legacy restore templateとは、shell評価を必要とする既存の文字列型resume commandを指す | legacy restore template |
| ユーザー依頼 | 通常起動とrestore起動の権限差を防ぐ | Codex restore argvがCodex起動argvの実行ファイルと通常起動オプションを順序込みで保持する条件 | 目的 | restore commandを構築した後 | Codex起動一致とは、session再接続引数以外のargvが新規pane起動とrestoreで一致することを指す | Codex起動一致 |
| 回帰検証 | shell aliasやplatform別shell構文で起動一致が崩れないことを証明する | Unixの`command`、PowerShellのcall operatorとsingle quote、cmdではUTF-16LE PowerShell scriptの`-EncodedCommand`へ同じ構造化argvを変換する試験 | 手段 | 実装後 | shell方言検証とは、同じ引数列が各shellで別の引数へ展開されず、Unix aliasとcmdメタ文字展開も発生しないことを直接検証することを指す | shell方言検証 |
